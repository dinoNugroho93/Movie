import { StyleSheet } from "react-native";

const stylesList = StyleSheet.create({
    titleList: {
        color: "#E7BB42",
        fontWeight: "800",
        fontSize: 16
    },
})
export default stylesList;